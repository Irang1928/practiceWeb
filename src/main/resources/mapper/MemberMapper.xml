<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.irang.practiceweb.dao.MemberDao">
    <select id="selectAll" resultType="Member">
        SELECT * FROM member
    </select>
</mapper>